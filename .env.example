# Optional: Finviz Elite API key, used only for ad-hoc yield/screening lookups.
# The core backtest does NOT need this. Copy this file to `.env` and fill in your key.
API_KEY=your-finviz-elite-key-here
